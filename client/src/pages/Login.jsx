import Navbar from "../components/Navbar.jsx";

// I did not want to overload everyone with the fully working auth system I made already, so this will just be empty for the initial commit

export default function Login() {
    return (
        <>
            <Navbar />
            <div>
                <h1>Login</h1>
            </div>
        </>
    )
}